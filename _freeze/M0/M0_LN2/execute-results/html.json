{
  "hash": "f0fb1493a574cb6cb7ec999d19883a35",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: \"AD698 - Applied Generative AI\"\nsubtitle: \"Neural Network Math and SGD\"\nnumber-sections: true\ndate: \"2026-01-01\"\ndate-modified: today\ndate-format: long\nengine: jupyter\nbibliography: ../references.bib\ncategories: ['M0:', 'Mathematics', 'Neural Networks', 'SGD', 'Lecture Notes']\ndescription: \"Lecture notes for Module 0 covering Mathematical Foundations for Neural Networks and SGD.\"\n---\n\n# Generative AI Foundations: Colab and Tensors\n\nBefore diving into the tools, it's essential to understand the paradigm shift we are experiencing. Traditional machine learning models, often referred to as discriminative models, focus on predicting outputs by learning the conditional probability of some expected output given an input. They are adept at tasks like classification or regression. Generative Artificial Intelligence (GenAI), on the other hand, seeks to learn and replicate complex data distributions to synthesize entirely new and original data, mirroring human-like outputs. \n\nAccording to *Generative AI Foundations in Python*, state-of-the-art generative models can behave as collaborators capable of synthetic understanding and generating sophisticated responses. The rapid growth of generative approaches fundamentally reshapes how we interact with technology. Whether you are synthesizing images with Diffusion models or generating text with Large Language Models (LLMs) based on the Transformer architecture, these models require immense computational power. This is where tools like Google Colab become indispensable for modern AI development. Jupyter notebooks enable live code execution, visualization, and explanatory text, suitable for prototyping and data analysis. Google Colab is a cloud-based version of Jupyter Notebook specifically designed for machine learning prototyping. It provides free GPU resources and integrates directly with Google Drive for file storage and sharing, making it the perfect environment to handle the computational complexity of deep generative models.\n\n\n\n# Why this chapter\n\nThis lecture gives the mathematical foundation you need for the rest of AD698.\nThe goal is not abstract theorem proving; the goal is to make deep learning equations and code readable and intuitive.\n\nThe structure in this chapter is adapted from the preliminaries sequence in Dive into Deep Learning [@zhang2023d2l], with additional SGD intuition and workflow guidance from DataCamp's SGD tutorial [@datacamp_sgd_tutorial].\n\n\n# Learning outcomes\n\nBy the end of this lecture, you should be able to:\n\n1. Use vectors, matrices, and tensor shapes confidently.\n2. Explain derivatives, chain rule, and gradients in optimization.\n3. Describe and compare sigmoid, tanh, ReLU, and softmax.\n4. Explain why SGD works and what hyperparameters control behavior.\n5. Connect math expressions directly to PyTorch training code.\n\n\n# Mathematical preliminaries for deep learning\n\n## Scalars, vectors, matrices, tensors\n\nA scalar is a single value.\nA vector is an ordered list of values.\nA matrix is a 2D array.\nA tensor is a generalization to higher dimensions.\n\nIn deep learning, dimensions encode meaning:\n\n1. Batch dimension\n2. Feature/channel dimension\n3. Optional sequence/time and spatial dimensions\n\nA shape mismatch error is usually a math-model mismatch, not just a coding bug.\n\n::: {#shape-demo .cell execution_count=1}\n``` {.python .cell-code .code-overflow-wrap}\nimport torch\n\nx_vec = torch.arange(6)\nx_mat = torch.arange(12).reshape(3, 4)\nx_tensor3 = torch.arange(24).reshape(2, 3, 4)\n\nprint(\"vector shape:\", x_vec.shape)\nprint(\"matrix shape:\", x_mat.shape)\nprint(\"3D tensor shape:\", x_tensor3.shape)\n```\n\n::: {.cell-output .cell-output-stdout}\n```\nvector shape: torch.Size([6])\nmatrix shape: torch.Size([3, 4])\n3D tensor shape: torch.Size([2, 3, 4])\n```\n:::\n:::\n\n\n## Linear algebra intuition\n\nFor a linear model:\n\n$$\n\\begin{align}\n\\hat{y} = Xw + b\n\\end{align}\n$$\n\nwhere $X \\in \\mathbb{R}^{n \\times d}$, $w \\in \\mathbb{R}^{d}$, and $b \\in \\mathbb{R}$.\n\nIn neural networks, we repeat this linear transform many times with nonlinear functions in between.\n\n\n# Calculus for learning\n\n## Derivative and gradient\n\nA derivative gives local slope in 1D.\nA gradient is the vector of partial derivatives:\n\n$$\n\\begin{align}\n\\nabla f(\\theta) = \\left[\\frac{\\partial f}{\\partial \\theta_1}, \\dots, \\frac{\\partial f}{\\partial \\theta_p}\\right]^\\top\n\\end{align}\n$$\n\nThe negative gradient points toward steepest local decrease.\n\n## Chain rule and backpropagation\n\nIf $L$ depends on $z$, and $z$ depends on $w$:\n\n$$\n\\begin{align}\n\\frac{\\partial L}{\\partial w} = \\frac{\\partial L}{\\partial z}\\frac{\\partial z}{\\partial w}\n\\end{align}\n$$\n\nBackpropagation applies this rule layer-by-layer [@Goodfellow.Bengio.Courville.2016].\n\n\n# Activation functions and why they matter\n\nWithout nonlinear activations, stacked layers collapse to one linear map. Activations create expressive function classes.\n\n## Sigmoid\n\n$$\n\\begin{align}\n\\sigma(x)=\\frac{1}{1+e^{-x}}\n\\end{align}\n$$\n\nRange $(0,1)$, useful for binary probability outputs.\nCan saturate for large $|x|$ and produce tiny gradients.\n\n## Tanh\n\n$$\n\\begin{align}\n\\tanh(x)=\\frac{e^x-e^{-x}}{e^x+e^{-x}}\n\\end{align}\n$$\n\nRange $(-1,1)$, centered around zero.\nStill saturates at extremes.\n\n## ReLU\n\n$$\n\\begin{align}\n\\mathrm{ReLU}(x)=\\max(0,x)\n\\end{align}\n$$\n\nComputationally simple, sparse activations, strong default in hidden layers.  Potential issue: dead ReLU neurons.\n\n## Softmax\n\nFor logits $z_i$:\n\n$$\n\\begin{align}\n\\mathrm{softmax}(z_i)=\\frac{e^{z_i}}{\\sum_j e^{z_j}}\n\\end{align}\n$$\n\nMaps logits to a probability simplex for multiclass tasks.\n\n::: {#cell-fig-activations .cell execution_count=2}\n``` {.python .cell-code}\nimport numpy as np\nimport matplotlib.pyplot as plt\n\nx = np.linspace(-3, 3, 400)\nsigmoid = 1 / (1 + np.exp(-x))\ntanh = np.tanh(x)\nrelu = np.maximum(0, x)\n\nplt.figure(figsize=(6.5, 4.5))\nplt.plot(x, sigmoid, label=\"sigmoid\")\nplt.plot(x, tanh, label=\"tanh\")\nplt.plot(x, relu, label=\"relu\")\nplt.axhline(0, linewidth=1, linestyle=\"--\")\nplt.title(\"Common Activation Functions\", fontsize=12, fontweight=\"bold\", loc=\"left\")\nplt.xlabel(\"x\", fontsize=12, fontweight=\"bold\")\nplt.ylabel(\"activation(x)\", fontsize=12, fontweight=\"bold\")\nplt.legend()\nplt.grid(alpha=0.25)\nplt.show()\n```\n\n::: {.cell-output .cell-output-display}\n![Sigmoid, tanh, and ReLU on a shared input grid](M0_LN2_files/figure-html/fig-activations-output-1.png){#fig-activations width=565 height=415 fig-alt='Line plot comparing three activation functions over the range -3 to 3: sigmoid (S-curve bounded 0 to 1), tanh (S-curve bounded -1 to 1), and ReLU (zero for negative inputs, linear for positive inputs).'}\n:::\n:::\n\n\n::: {#cell-fig-softmax-bars .cell execution_count=3}\n``` {.python .cell-code}\nimport torch\nimport matplotlib.pyplot as plt\n\nlogits = torch.tensor([1.2, 0.3, -0.7, 2.0])\nprobs = torch.softmax(logits, dim=0)\n\nplt.figure(figsize=(6.5, 4.5))\nplt.bar(range(len(probs)), probs.numpy(), width =0.5)\nplt.title(\"Softmax Output\", fontsize=12, fontweight=\"bold\", loc=\"left\")\nplt.xlabel(\"Class index\", fontsize=12, fontweight=\"bold\")\nplt.ylabel(\"Probability\", fontsize=12, fontweight=\"bold\")\nplt.ylim(0, 1)\nplt.grid(axis=\"y\", alpha=0.25)\nplt.show()\n\nprint(\"Probabilities:\", probs)\nprint(\"Sum:\", probs.sum())\n```\n\n::: {.cell-output .cell-output-display}\n![Softmax converts logits into class probabilities](M0_LN2_files/figure-html/fig-softmax-bars-output-1.png){#fig-softmax-bars width=554 height=415 fig-alt='Side-by-side bar charts: left shows four raw logit values; right shows the corresponding softmax probabilities that sum to 1, with class 3 (logit 2.0) receiving the highest probability.'}\n:::\n\n::: {.cell-output .cell-output-stdout}\n```\nProbabilities: tensor([0.2644, 0.1075, 0.0396, 0.5885])\nSum: tensor(1.0000)\n```\n:::\n:::\n\n\n# Gradient descent and stochastic gradient descent\n\n## Objective\n\nTraining minimizes empirical risk:\n\n$$\n\\begin{align}\n\\min_\\theta \\; J(\\theta)=\\frac{1}{n}\\sum_{i=1}^{n}\\ell(f_\\theta(x_i), y_i)\n\\end{align}\n$$\n\n## Full-batch gradient descent\n\n$$\n\\begin{align}\n\\theta_{t+1}=\\theta_t-\\eta\\nabla J(\\theta_t)\n\\end{align}\n$$\n\nUses all data each step. Stable but expensive for large datasets.\n\n## SGD and mini-batch SGD\n\nSGD uses one sample (or a mini-batch) per update:\n\n$$\n\\begin{align}\n\\theta_{t+1}=\\theta_t-\\eta\\nabla J_{\\mathcal{B}_t}(\\theta_t)\n\\end{align}\n$$\n\nwhere $\\mathcal{B}_t$ is a sampled mini-batch.\n\nWhy this works in practice [@datacamp_sgd_tutorial]:\n\n1. Updates are cheaper.\n2. Noise can help escape poor sharp regions.\n3. Scales well to large datasets.\n\nPractical controls:\n\n1. Learning rate $\\eta$\n2. Batch size\n3. Number of epochs\n4. Learning-rate schedule\n\n::: {#cell-fig-sgd-path .cell execution_count=4}\n``` {.python .cell-code}\nimport numpy as np\nimport matplotlib.pyplot as plt\n\nrng = np.random.default_rng(42)\n\ndef grad(theta):\n    x, y = theta\n    return np.array([2*(x-2), 8*(y+1)])\n\ndef sgd(theta0, lr=0.08, steps=60, noise_scale=0.2):\n    path = [theta0.copy()]\n    theta = theta0.copy()\n    for _ in range(steps):\n        g = grad(theta)\n        noise = rng.normal(0, noise_scale, size=2)\n        theta = theta - lr * (g + noise)\n        path.append(theta.copy())\n    return np.array(path)\n\npath_small_lr = sgd(np.array([4.5, 2.5]), lr=0.04, steps=70, noise_scale=0.2)\npath_large_lr = sgd(np.array([4.5, 2.5]), lr=0.18, steps=70, noise_scale=0.2)\n\nx = np.linspace(-1, 5, 200)\ny = np.linspace(-3, 3, 200)\nX, Y = np.meshgrid(x, y)\nZ = (X-2)**2 + 4*(Y+1)**2\n\nfig, axes = plt.subplots(2, 1, figsize=(5.75, 10), sharex=True, sharey=True)\nfor ax, path, ttl in [\n    (axes[0], path_small_lr, \"Smaller learning rate\"),\n    (axes[1], path_large_lr, \"Larger learning rate\"),\n]:\n    ax.contour(X, Y, Z, levels=25)\n    ax.plot(path[:, 0], path[:, 1], marker=\"o\", markersize=2)\n    ax.scatter([2], [-1], c=\"red\", label=\"minimum\")\n    ax.set_title(ttl, fontsize=12, fontweight=\"bold\", loc=\"left\")\n    ax.set_xlabel(r'$\\theta_1$', fontsize=12, fontweight=\"bold\")\n    ax.set_ylabel(r'$\\theta_2$', fontsize=12, fontweight=\"bold\")\n    ax.grid(alpha=0.2)\n\nplt.tight_layout()\nplt.show()\n```\n\n::: {.cell-output .cell-output-display}\n![Mini-batch SGD trajectories on a convex toy objective](M0_LN2_files/figure-html/fig-sgd-path-output-1.png){#fig-sgd-path width=540 height=948 fig-alt='Contour plot of a bowl-shaped loss surface with noisy optimization paths converging toward the minimum at the center, illustrating stochastic gradient descent behavior.'}\n:::\n:::\n\n\nInterpretation:\n\n1. Too small learning rate converges slowly.\n2. Too large learning rate oscillates or diverges.\n3. Moderate values usually give best progress per wall-clock time.\n\n\n# From math to PyTorch training loop\n\n::: {#371852a4 .cell execution_count=5}\n``` {.python .cell-code}\nimport torch\n\nmodel = torch.nn.Sequential(\n    torch.nn.Linear(2, 16),\n    torch.nn.ReLU(),\n    torch.nn.Linear(16, 2)\n)\n\nloss_fn = torch.nn.CrossEntropyLoss()\noptimizer = torch.optim.SGD(model.parameters(), lr=0.05)\n\nfor epoch in range(20):\n    optimizer.zero_grad()        # clear previous gradients\n    logits = model(X_batch)      # forward\n    loss = loss_fn(logits, y_batch)\n    loss.backward()              # backprop via chain rule\n    optimizer.step()             # gradient update\n```\n:::\n\n\nEach line maps directly to the equations above.\n\n\n# Common beginner mistakes\n\n1. Shape mismatch between logits and labels.\n2. Applying `softmax` before `CrossEntropyLoss` in PyTorch.\n3. Forgetting `optimizer.zero_grad()` each step.\n4. Using a learning rate that is too high.\n5. Treating decreasing training loss as sufficient evidence of generalization.\n\n\n# Summary\n\nYou now have a practical mathematical bridge from:\n\n1. Tensor structure\n2. Calculus and gradients\n3. Nonlinear activations\n4. SGD optimization\n5. Training loop implementation\n\nThese ideas will drive every model we build in later modules.\n\n\n# References\n\n",
    "supporting": [
      "M0_LN2_files"
    ],
    "filters": [],
    "includes": {}
  }
}